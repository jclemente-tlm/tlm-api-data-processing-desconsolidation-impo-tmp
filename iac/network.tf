# Este proyecto utiliza infraestructura de red existente (VPC, subnets, security groups).
# Los IDs se inyectan mediante variables definidas en variables.tf y los archivos .tfvars por ambiente.
# Si en el futuro se requiere crear recursos de red (VPC, subnets, SG, etc.), hacerlo aquí.
